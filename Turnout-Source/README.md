Extend the `Mimic Panel` module to stack a `Turnout Source` module on top. To be stack up an Arduino.

Should also pack and simplify the button inputs and LED signal outputs using connector more robust and to be stored in a dedicated case.